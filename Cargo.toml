[package]
name = "gatepost"
version = "0.1.0"
edition = "2024"
description = "GatePOST detects MCP schema drift against approved snapshots."
license = "MIT"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
